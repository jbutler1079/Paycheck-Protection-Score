# Paycheck-Protection-Score
Paycheck Protection Score™ (PPS) estimates how much healthcare out-of-pocket (OOP) spending consumes wages under multiple utilization scenarios, segmented by 4 income bands. It calculates a Financial Toxicity Index (FTI) and converts that into a simple 0–100 PPS where higher = better protection.
